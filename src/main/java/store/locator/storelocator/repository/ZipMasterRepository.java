package store.locator.storelocator.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import store.locator.storelocator.model.ZipMaster;

public interface ZipMasterRepository extends JpaRepository<ZipMaster, Integer> {

}
